# Python build artifacts from `pip install .` (the packaging shim in
# pyproject.toml that lets pre-commit's language: python install this repo
# as check-in-hygiene's hook source).
build/
*.egg-info/
__pycache__/

# continual-learning hook's local per-repo telemetry DB — machine-local
# state, not meant to be committed (see skills-plugins-hooks-agents'
# hooks/continual-learning/README.md).
.claude/learnings.db
